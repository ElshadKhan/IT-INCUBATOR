export type SessionDBType = {
    ip: string
    title: string
    lastActiveDate: Date
    expiredDate: Date
    deviceId: string
    userId: string
}
export type SessionType = {
    ip: string
    title: string
    lastActiveDate: Date
    deviceId: string
}
